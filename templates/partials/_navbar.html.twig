<nav class="navbar navbar-expand-lg">

	<div class="container navbar-container">

		<a class="navbar-brand" href="{{ path('home') }}">

			<img src="{{ asset('images/logo/logo.svg') }}" alt="Logo Rapide & Gourmand" class="navbar-logo">

			<div class="navbar-brand-text">

				<span>Rapide &</span>
				<span>Gourmand</span>

			</div>

		</a>

		<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Ouvrir le menu">
			<span class="navbar-toggler-icon"></span>
		</button>

		<div class="collapse navbar-collapse" id="mainNavbar">

			<ul class="navbar-nav">

				<li class="nav-item">
					<a class="nav-link" href="{{ path('home') }}">
						Accueil
					</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="{{ path('menu_index') }}">
						Menus
					</a>
				</li>

				<li class="nav-item">
					<a class="nav-link" href="#">
						Contact
					</a>
				</li>

				<li id="employee-space-item" class="nav-item d-none">
					<a class="nav-link" href="{{ path('employee_dashboard') }}">
						Espace professionnel
					</a>
				</li>

			</ul>

			<div class="navbar-actions">

				<a id="login-link" href="{{ path('app_signin') }}" class="btn btn-primary-custom">
					Se connecter
				</a>

				<a id="profile-link" href="{{ path('profile_index') }}" class="btn btn-primary-custom d-none">
					Mon profil
				</a>

				<button id="logout-button" type="button" class="btn btn-outline-light d-none">
					Déconnexion
				</button>

			</div>

		</div>

	</div>

</nav>
